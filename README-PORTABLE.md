# ServiceCenterApp - Портативна Версія

## 📦 Опис
ServiceCenterApp - це Electron додаток для керування сервісним центром. Проект налаштовано для створення портативних версій, які можна запускати на різних платформах без встановлення додаткових програм.

## 🚀 Швидкий старт

### Для користувачів (запуск готової збірки)

#### Linux
```bash
# Надайте права на виконання та запустіть
chmod +x run-linux.sh
./run-linux.sh
```

Або напряму запустіть AppImage:
```bash
chmod +x release/0.0.0/ServiceCenterApp-Linux-0.0.0.AppImage
./release/0.0.0/ServiceCenterApp-Linux-0.0.0.AppImage
```

#### Windows
Просто подвійно клікніть на файл `run-windows.bat` або запустіть напряму:
```
release\0.0.0\ServiceCenterApp-Windows-0.0.0-Portable.exe
```

**Важливо для Windows**: Портативна версія (.exe) не потребує встановлення! Просто запустіть файл.

## 💻 Для розробників

### Мінімальні залежності
Для збірки проекту потрібно:
- **Node.js** (версія 18 або новіша) - [завантажити](https://nodejs.org/)
- **npm** (входить в комплект з Node.js)

### Встановлення залежностей
```bash
npm install
```

### Запуск у режимі розробки
```bash
npm run dev
```

### Створення портативних збірок

#### Збірка для поточної платформи
```bash
npm run build
```

#### Збірка тільки для Windows (portable)
```bash
npm run build:win
```

#### Збірка тільки для Linux (AppImage)
```bash
npm run build:linux
```

#### Збірка для всіх платформ
```bash
npm run build:all
```

**Примітка**: Для збірки Windows версії на Linux може знадобитися wine та додаткові інструменти.

## 📁 Структура проекту
```
service-center-app/
├── dist/              # Зібрані веб-файли
├── dist-electron/     # Зібрані Electron файли
├── electron/          # Вихідний код Electron
├── src/               # Вихідний код React додатку
├── release/           # Готові портативні збірки
│   └── 0.0.0/
│       ├── ServiceCenterApp-Linux-0.0.0.AppImage
│       └── ServiceCenterApp-Windows-0.0.0-Portable.exe
├── run-linux.sh       # Скрипт запуску для Linux
├── run-windows.bat    # Скрипт запуску для Windows
└── electron-builder.json5  # Конфігурація збірки
```

## 🔧 Конфігурація

### Формати збірок
- **Linux**: AppImage - самодостатній виконуваний файл, не потребує встановлення
- **Windows**: 
  - Portable (.exe) - портативна версія без встановлення
  - NSIS Setup (.exe) - інсталятор для встановлення

### База даних
Проект використовує SQLite (файл `1.sqlite`), який зберігається в папці проекту. При копіюванні проекту база даних переноситься разом з ним.

## 📋 Системні вимоги

### Linux
- Сучасний дистрибутив Linux (Ubuntu 20.04+, Fedora 35+, тощо)
- FUSE (для AppImage) - зазвичай вже встановлено

### Windows
- Windows 10 або новіша
- Немає додаткових вимог для portable версії

## 🎯 Особливості портативності
- ✅ Не потребує прав адміністратора для запуску
- ✅ База даних зберігається в папці проекту
- ✅ Всі налаштування локальні
- ✅ Можна запускати з USB носія
- ✅ Легко переносити між комп'ютерами

## 📝 Ліцензія
Приватний проект

## 🤝 Підтримка
Для питань та підтримки звертайтесь до розробника.
