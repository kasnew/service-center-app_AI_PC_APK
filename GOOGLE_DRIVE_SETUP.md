# Покрокова інструкція: Налаштування Google Drive

## Крок 1: Створення проекту в Google Cloud Console

1. Перейдіть на https://console.cloud.google.com/
2. Увійдіть у свій Google акаунт
3. Натисніть на випадаючий список проектів у верхній панелі
4. Натисніть **"NEW PROJECT"** (або "Створити проект")
5. Введіть назву проекту: `Service Center App` (або будь-яку іншу)
6. Натисніть **"CREATE"** (або "Створити")
7. Дочекайтеся створення проекту (зазвичай кілька секунд)

## Крок 2: Увімкнення Google Drive API

1. У лівому меню виберіть **"APIs & Services"** → **"Library"** (або "Бібліотека")
2. У пошуковому полі введіть: `Google Drive API`
3. Натисніть на **"Google Drive API"** з результату пошуку
4. Натисніть кнопку **"ENABLE"** (або "Увімкнути")
5. Дочекайтеся активації API (зазвичай миттєво)

## Крок 3: Налаштування OAuth Consent Screen

1. У лівому меню виберіть **"APIs & Services"** → **"OAuth consent screen"** (або "Екран згоди OAuth")
2. Виберіть **"External"** (Зовнішній) для User Type (якщо немає Google Workspace)
3. Натисніть **"CREATE"** (Створити)
4. Заповніть форму:
   - **App name**: `Service Center App` (або будь-яка назва)
   - **User support email**: Ваш email
   - **Developer contact information**: Ваш email
5. Натисніть **"SAVE AND CONTINUE"** (Зберегти і продовжити)
6. На екрані **"Scopes"**:
   - Натисніть **"ADD OR REMOVE SCOPES"**
   - Знайдіть і виберіть: `https://www.googleapis.com/auth/drive.file`
   - Натисніть **"UPDATE"** → **"SAVE AND CONTINUE"**
7. На екрані **"Test users"** (якщо показано):
   - Додайте свій email як тестового користувача (якщо проект у тестовому режимі)
   - Натисніть **"SAVE AND CONTINUE"**
8. На екрані **"Summary"**:
   - Перевірте інформацію
   - Натисніть **"BACK TO DASHBOARD"**

## Крок 4: Створення OAuth 2.0 Credentials

1. У лівому меню виберіть **"APIs & Services"** → **"Credentials"** (або "Облікові дані")
2. Натисніть **"+ CREATE CREDENTIALS"** (або "Створити облікові дані")
3. Виберіть **"OAuth client ID"** (або "Ідентифікатор клієнта OAuth")
4. Якщо з'явиться попередження про налаштування OAuth consent screen - натисніть **"CONFIGURE CONSENT SCREEN"** і виконайте Крок 3
5. У формі створення credentials:
   - **Application type**: Виберіть **"Desktop app"** (або "Десктопний додаток")
   - **Name**: `Service Center Desktop Client` (або будь-яка назва)
6. Натисніть **"CREATE"** (Створити)
7. З'явиться вікно з **Client ID** та **Client secret**
8. Натисніть **"DOWNLOAD JSON"** (або "Завантажити JSON")
9. Файл завантажиться з назвою типу `client_secret_XXXXX.json`

## Крок 5: Підготовка credentials.json

1. Перейменуйте завантажений файл в `credentials.json`
2. Відкрийте файл у текстовому редакторі
3. Перевірте структуру - вона повинна виглядати так:

```json
{
  "installed": {
    "client_id": "ваш-client-id.apps.googleusercontent.com",
    "project_id": "ваш-project-id",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "ваш-client-secret",
    "redirect_uris": ["http://localhost"]
  }
}
```

4. Якщо структура відрізняється (наприклад, `web` замість `installed`), це теж працюватиме - код підтримує обидва формати

## Крок 6: Розміщення credentials.json

1. Скопіюйте файл `credentials.json` в одну з наступних локацій (програма шукатиме в такому порядку):
   - Корінь проекту (там, де знаходиться `package.json`)
   - Папка `electron/` (там, де знаходяться файли `main.ts`, `ipcHandlers.ts`)
   - Папка userData (автоматично визначається Electron)

**Рекомендовано**: Покласти в корінь проекту або в папку `electron/`

## Крок 7: Перевірка в програмі

1. Запустіть програму
2. Перейдіть в **Settings** → вкладка **"Google Drive"**
3. Перевірте статус:
   - Якщо бачите "Credentials не налаштовано" - перевірте, чи файл `credentials.json` знаходиться в правильній папці
   - Якщо бачите кнопку "Підключити Google Drive" - все готово!

## Крок 8: Авторизація

1. Натисніть **"Підключити Google Drive"**
2. Відкриється вікно браузера з Google авторизацією
3. Виберіть Google акаунт
4. Натисніть **"Allow"** (Дозволити) для надання доступу
5. Вікно закриється автоматично
6. У програмі з'явиться статус "Підключено до Google Drive"

## Готово! 🎉

Тепер ви можете:
- Завантажувати локальні бекапи в Google Drive
- Завантажувати бекапи з Google Drive
- Видаляти бекапи з Google Drive

---

## Усунення проблем

### Помилка: "Credentials not found"
- Перевірте, чи файл `credentials.json` знаходиться в одній з рекомендованих папок
- Перевірте назву файлу (має бути точно `credentials.json`)
- Перевірте формат JSON файлу (має бути валідним JSON)

### Помилка: "Access blocked: This app's request is invalid"
- Перевірте, чи правильно налаштований OAuth consent screen
- Переконайтеся, що додали свій email як тестового користувача (якщо проект у тестовому режимі)
- Перевірте, чи правильно вказані scopes в OAuth consent screen

### Помилка: "redirect_uri_mismatch"
- Перевірте, чи в credentials.json є `redirect_uris` з `http://localhost`
- Якщо ні, додайте: `"redirect_uris": ["http://localhost"]`

### Помилка авторизації
- Перевірте інтернет-з'єднання
- Спробуйте відключитися і підключитися знову
- Перевірте, чи не застаріли токени (видаліть `google_drive_token.json` в userData папці)

---

## Безпека

⚠️ **ВАЖЛИВО:**
- Ніколи не комітьте `credentials.json` в git (вже додано в `.gitignore`)
- Не діліться `credentials.json` з іншими
- Якщо credentials потрапили в чужий доступ - негайно видаліть їх в Google Cloud Console і створіть нові

