# Діагностика підключення

## ✅ Сервер працює правильно!

Перевірка через термінал показала:
- ✅ HTTP 200 OK
- ✅ Відповідь: `{"status":"ok","timestamp":"..."}`
- ✅ Сервер слухає на `0.0.0.0:3000` (всі інтерфейси)
- ✅ IP адреса: `192.168.0.101`

## 🔍 Як перевірити в Android додатку

### 1. Перевірте логи Android додатку

**Через Android Studio:**
1. Підключіть телефон через USB
2. Відкрийте Android Studio
3. Відкрийте вкладку **Logcat**
4. Відфільтруйте по: `SettingsViewModel`
5. Спробуйте підключитися в додатку
6. Подивіться на логи - там буде детальна інформація про помилку

**Через ADB:**
```bash
adb logcat | grep -i "SettingsViewModel\|ApiClient\|OkHttp"
```

### 2. Перевірте налаштування в додатку

1. Відкрийте Android додаток
2. Перейдіть в **Налаштування**
3. Перевірте поле **"URL сервера"**:
   - Має бути: `http://192.168.0.101:3000`
   - БЕЗ пробілів на початку/в кінці
   - З `http://` на початку
4. Натисніть **"Зберегти"**
5. Натисніть **"Перевірити"**

### 3. Можливі проблеми

#### Проблема: "UnknownHostException"
**Причина:** Неправильна IP адреса або телефон не в тій же мережі
**Рішення:**
- Перевірте, що телефон підключений до того ж Wi-Fi, що й комп'ютер
- Перевірте IP адресу в налаштуваннях Electron додатку

#### Проблема: "ConnectException" або "Connection refused"
**Причина:** Сервер не запущений або firewall блокує
**Рішення:**
- Перевірте, що сервер запущений в Electron додатку
- Перевірте firewall на комп'ютері

#### Проблема: "SocketTimeoutException"
**Причина:** Мережа повільна або блокуються пакети
**Рішення:**
- Перевірте, що обидва пристрої в одній мережі
- Спробуйте перезапустити Wi-Fi на телефоні

### 4. Тест через браузер телефону

**Найпростіший спосіб перевірити:**

1. Відкрийте браузер на телефоні (Chrome, Firefox)
2. Введіть в адресному рядку:
   ```
   http://192.168.0.101:3000/api/health
   ```
3. Повинен з'явитися JSON:
   ```json
   {"status":"ok","timestamp":"2025-12-02T..."}
   ```

**Якщо не відкривається в браузері:**
- Проблема в мережі або firewall
- Перевірте, що телефон в тій же Wi-Fi мережі

**Якщо відкривається в браузері, але не в додатку:**
- Проблема в коді Android додатку
- Перевірте логи через Logcat

### 5. Оновлений код

Я додав детальне логування в Android додаток. Після оновлення додатку:

1. Перевірте логи через Logcat
2. Подивіться на повідомлення типу:
   - `Checking connection to: http://192.168.0.101:3000`
   - `Response code: 200, isSuccessful: true`
   - Або помилку з деталями

## 📱 Оновлення додатку

Після змін потрібно перезібрати та встановити нову версію:

```bash
cd android-app
./gradlew assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

Або через Android Studio: **Build → Rebuild Project**


